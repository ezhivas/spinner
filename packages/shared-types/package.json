{
  "name": "@spinner/shared-types",
  "version": "0.0.1",
  "description": "Shared TypeScript types and DTOs for SpinneR API Client",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "class-validator": "^0.14.3",
    "class-transformer": "^0.5.1"
  }
}
